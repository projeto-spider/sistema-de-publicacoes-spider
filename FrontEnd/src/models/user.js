export default class User {
  constructor(username, email, password, name, phone, status) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.name = name; 
    this.phone = phone;
    this.status = status;
  }
}
